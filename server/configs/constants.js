// Server Routing Constants
const SERVER_ROUTE_URL = {
    HOME: '/',
    JS_FILES: '/js',
    API: '/api',
};

const DEFAULT_SERVER_PORT = 3000;

// Server API Routing URLs
const SERVER_API_ROUTE_URL = {
    GET_PHONE_NUMBERS: '/phone/number',
};

module.exports = {
    SERVER_ROUTE_URL,
    DEFAULT_SERVER_PORT,
    SERVER_API_ROUTE_URL,
};
